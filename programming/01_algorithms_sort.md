# プログラミング: ソートアルゴリズム

**理解度**: 0%

## 概要

ソートアルゴリズムの選択はプログラムの効率性を大きく変えます。

## マインドマップ
## キーワード

### バブルソート
隣同士の要素を比較して交換。シンプルだが遅い。

### クイックソート
分割統治を使う。平均 O(n log n)。

## 3行要約

1. ソートには計算量が異なるアルゴリズムがある
2. O(n²) は小さいデータ、O(n log n) は大きいデータに向く
3. 過去問では「どのソートを選ぶか」が問われる
